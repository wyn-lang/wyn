fn main() {
    print("Testing OS syscalls...")
    
    # Test getpid() - syscall 20
    const pid: int = syscall(20, 0, 0, 0, 0, 0, 0)
    if pid > 0 {
        print("✓ getpid() works - got valid PID")
    }
    
    print("✓ All OS syscall tests passed!")
}
