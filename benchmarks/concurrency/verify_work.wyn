fn compute(n: int) {
    let mut sum = 0
    let mut i = 0
    while i < n {
        sum = sum + i * i
        i = i + 1
    }
    print("Sum: ")
    print(sum)
}

fn main() {
    spawn { compute(100000) }
    spawn { compute(100000) }
    spawn { compute(100000) }
    
    print("Done")
}
