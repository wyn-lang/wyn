import std/io

fn fib(n: int) -> int {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    const result: int = fib(35)
    io.print("fib(35) = ")
    print(result)
    io.print("")
}
