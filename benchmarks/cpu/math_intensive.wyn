fn main() {
    let mut sum = 0.0
    let mut i = 0
    
    while i < 10000000 {
        let x = int_to_float(i)
        sum = sum + sinf(x) + cosf(x) + sqrtf(x)
        i = i + 1
    }
    
    print_str("Math result: ")
    print_float(sum)
    print_newline()
}
