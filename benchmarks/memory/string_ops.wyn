import std/io

fn main() {
    let result: str = ""
    let i: int = 0
    
    while i < 10000 {
        result = str_concat(result, "x")
        i = i + 1
    }
    
    io.print_str("String concat: ")
    print(result.len())
    io.print_newline()
}
