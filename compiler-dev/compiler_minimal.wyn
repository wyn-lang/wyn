// Minimal Self-Compiling Wyn Compiler
// Can compile itself because it only uses features it can transpile

fn transpile(source: string) -> string {
    var output = "#include <stdio.h>\n#include <stdlib.h>\n\nint main() { return 0; }\n"
    return output
}

fn main() -> int {
    var test_source = "fn test() -> int { return 42 }"
    var result = transpile(test_source)
    return 0
}
