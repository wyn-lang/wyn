[package]
name = "aws"
version = "0.1.0"
description = "AWS SDK for Wyn - Basic AWS operations"
license = "MIT"
author = "Wyn Team"

[dependencies]
# No external dependencies - uses AWS CLI

[build]
entry = "main.wyn"
