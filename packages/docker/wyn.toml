[package]
name = "docker"
version = "0.1.0"
description = "Docker API for Wyn - Container management"
license = "MIT"
author = "Wyn Team"

[dependencies]
# Requires docker command-line tool

[build]
entry = "main.wyn"
