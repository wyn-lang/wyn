[package]
name = "postgres"
version = "0.1.0"
description = "PostgreSQL driver for Wyn - Database operations"
license = "MIT"
author = "Wyn Team"

[dependencies]
# Requires psql command-line tool

[build]
entry = "main.wyn"
