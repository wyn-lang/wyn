fn main() {
    const a: str = "Hello"
    const b: str = "World"
    const result: str = a + " " + b
    
    print("Testing string concatenation:")
    print(result)
    
    # Test with assert - but we can't use assert with string comparison yet
    # assert(result == "Hello World")
    
    print("String concatenation test completed!")
}
