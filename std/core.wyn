# std/core.wyn
# Core functions - must be imported to use

# Print a string
fn print(s: str) {
    print(s)
}

# Get length of array or string  
fn len(arr: [any]) -> int {
    return len(arr)
}

# Check a condition is true (assert)
fn check(condition: bool) {
    assert(condition)
}
