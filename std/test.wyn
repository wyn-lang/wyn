# std/test.wyn
# Testing utilities

# Assert a condition is true
fn assert(condition: bool) {
    assert(condition)
}

# Assert with message
fn assert_eq(a: int, b: int, msg: str) {
    if a != b {
        # Would print error but we can't use print yet
        assert(false)
    }
}
