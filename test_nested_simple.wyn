struct Inner {
    value: int
}

struct Outer {
    inner: Inner
    count: int
}

fn main() {
    let inner: Inner = Inner { value: 42 }
    let outer: Outer = Outer { inner: inner, count: 10 }
    
    print(outer.count)
    print_newline()
    print(outer.inner.value)
    print_newline()
}
