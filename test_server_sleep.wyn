# Test tcp_listen - keeps socket open for 2 seconds

fn main() {
    let listen_fd: int = tcp_listen(9999)
    
    if listen_fd < 0 {
        exit(1)
    }
    
    print(listen_fd)
    print_newline()
    
    # Keep socket open for 2 seconds
    sleep(2)
    
    tcp_close(listen_fd)
}
