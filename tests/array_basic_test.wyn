
import test

fn main() {
    const arr: [int] = [1, 2, 3, 4, 5]
    
    # Length
    test.assert(arr.len() == 5)
    
    # Indexing
    test.assert(arr[0] == 1)
    test.assert(arr[1] == 2)
    test.assert(arr[2] == 3)
    test.assert(arr[3] == 4)
    test.assert(arr[4] == 5)
    
    # Negative indexing
    test.assert(arr[-1] == 5)
    test.assert(arr[-2] == 4)
    
    print("✓ Basic array test passed")
}
