// Test: Auto-import core modules (no import statements needed)

fn main() {
    // Test array module (auto-imported)
    var arr = []
    arr = array.append(arr, 1)
    print(arr.len())  // 1
    
    // Test string module (auto-imported)
    var upper = string.upper("hello")
    print(upper)  // HELLO
}
