import test
import crypto


fn main() {
    print("Testing crypto stdlib...\n")
    
    # Test XOR properties
    const a: int = 42
    const b: int = 17
    const xor1: int = a ^ b
    const xor2: int = xor1 ^ b
    test.assert(xor2 == a, "XOR is reversible")
    
    # Test XOR with zero
    test.assert((a ^ 0) == a, "XOR with 0")
    
    # Test XOR with self
    test.assert((a ^ a) == 0, "XOR with self is 0")
    
    # Test XOR commutativity
    test.assert((a ^ b) == (b ^ a), "XOR is commutative")
    
    print("✓ All crypto stdlib tests passed\n")
}
