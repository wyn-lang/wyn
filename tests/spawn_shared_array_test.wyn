
fn main() {
    test_multiple_variables()
}

fn test_multiple_variables() {
    var v0: int = 0
    var v1: int = 0
    var v2: int = 0
    var v3: int = 0
    var v4: int = 0
    
    spawn {
        v0 = 0
    }
    
    spawn {
        v1 = 10
    }
    
    spawn {
        v2 = 20
    }
    
    spawn {
        v3 = 30
    }
    
    spawn {
        v4 = 40
    }
    
    task_join_all()
    assert(v0 == 0)
    assert(v1 == 10)
    assert(v2 == 20)
    assert(v3 == 30)
    assert(v4 == 40)
}
