
fn main() {
    test_multiple_variables()
}

fn test_multiple_variables() {
    let v0: int = 0
    let v1: int = 0
    let v2: int = 0
    let v3: int = 0
    let v4: int = 0
    
    spawn {
        v0 = 0
    }
    
    spawn {
        v1 = 10
    }
    
    spawn {
        v2 = 20
    }
    
    spawn {
        v3 = 30
    }
    
    spawn {
        v4 = 40
    }
    
    task_join_all()
    assert(v0 == 0)
    assert(v1 == 10)
    assert(v2 == 20)
    assert(v3 == 30)
    assert(v4 == 40)
}
