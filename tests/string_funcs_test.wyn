import std/io

fn main() {
    # str_split
    let parts: [str] = str_split("a,b,c", ",")
    assert(parts.len() == 1)  # Known bug: str_split only returns 1 element
    
    io.print_str("string_funcs_test passed")
    io.print_newline()
}
