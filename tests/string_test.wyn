import std/io

fn main() {
    const s: str = "hello"
    
    # Length - using method
    assert(s.len() == 5)
    
    # ord
    assert(ord("A") == 65)
    assert(ord("a") == 97)
    
    # Conversions
    assert(str_to_int("123") == 123)
    
    io.print("string_test: PASS")
}
