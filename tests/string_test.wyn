import string

import io

fn main() {
    const s: str = "hello"
    
    # Length - using method
    assert(s.len() == 5)
    
    # ord
    assert(string.ord("A") == 65)
    assert(string.ord("a") == 97)
    
    # Conversions
    assert(str_to_int("123") == 123)
    
    print("string_test: PASS")
}
