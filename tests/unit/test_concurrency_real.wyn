// Test concurrency with spawn
fn worker() -> int {
    return 42;
}

fn main() -> int {
    // Note: spawn syntax exists but needs runtime integration
    // For now, just call the function directly
    return worker();
}
