type UserId = int;
type Score = int;
type Distance = int;

fn main() -> int {
    let user: UserId = 100;
    let score: Score = 95;
    let dist: Distance = 50;
    
    return user + score + dist;  // 245
}
